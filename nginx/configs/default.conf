upstream backend {
  server backend:4000;
}


upstream client {
  server client:3000;
}


server {

  listen 80;

  location / {
    proxy_pass http://client;
  }

  location /api/ {
    proxy_pass http://backend/api;
  }

}